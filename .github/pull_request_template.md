## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] All my commits are signed off (`git commit -s`) per the
      [DCO](../DCO.txt).
- [ ] `cargo build` and `cargo test` pass (backend).
- [ ] `pnpm build` passes (frontend), if touched.
- [ ] Server-side validation added for any new API input.
- [ ] Changes are focused and match surrounding code style.
