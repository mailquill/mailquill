apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailquill.fullname" . }}
  labels: {{ include "mailquill.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector: {{ include "mailquill.selectorLabels" . | nindent 4 }}
