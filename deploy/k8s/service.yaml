apiVersion: v1
kind: Service
metadata:
  name: mailquill
  namespace: mailquill
spec:
  type: ClusterIP
  selector:
    app: mailquill
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
